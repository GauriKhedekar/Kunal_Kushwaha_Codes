1)Prime numbers:-
public class Main {
    public static void main(String[] args) {
        int n = 40;
        boolean[] prime = new boolean[n];

        // assume all prime
        for (int i = 0; i < n; i++) {
            prime[i] = true;
        }

        prime[0] = prime[1] = false;

        for (int i = 2; i * i < n; i++) {
            if (prime[i]) {
                // start from i*i (not 2*i)
                for (int j = i * i; j < n; j += i) {
                    prime[j] = false;
                }
            }
        }

        for (int i = 2; i < n; i++) {
            if (prime[i]) {
                System.out.println(i);
            }
        }
    }
}

❓ Your Doubt (Clear Answer)
Why NOT 2*i? Why i*i?
Multiples < i² already have a smaller prime factor
They were marked earlier
Starting from 2*i causes duplicate marking
i*i avoids redundant work → optimized

🔹 Method Used
Sieve of Eratosthenes
🔹 Why it is called “Sieve”?
Like a sieve/strainer, it:
keeps prime numbers
filters out (marks) multiples (composites)

⏱ Time Complexity (Short & Exact)
Time Complexity
✅ O(n log log n)
Why? (Harmonic Progression)
Total work:
(1)n/2 + (1)n/3 + (1)n/5 + (1)n/7 + ...
= n (1/2 + 1/3 + 1/5 + ...)
≈ n log log n
📌 This is a harmonic series over primes

📦 Space Complexity
✅ O(n)

2)Sqrt of a number:-
